{
  "scripts": {
    "test": "mocha tests --recursive",
    "cover": "./node_modules/.bin/istanbul cover ./node_modules/mocha/bin/_mocha -- -R spec tests --recursive",
    "report": "./node_modules/codeclimate-test-reporter/bin/codeclimate.js < ./coverage/lcov.info"
  },
  "engines": {
    "node": ">= 4"
  },
  "dependencies": {
    "promise": "^7.1.1"
  },
  "devDependencies": {
    "codeclimate-test-reporter": "^0.3.1",
    "istanbul": "^0.4.2",
    "mocha": "^2.4.5"
  }
}
